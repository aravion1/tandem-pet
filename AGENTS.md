# Контекст проекта

Перед началом выполнения каждой задачи обязательно изучай содержимое каталога
`project-context/` и планируй работу с учётом описанного там контекста проекта.
Если контекст влияет на решения, явно учитывай его в плане и реализации.
